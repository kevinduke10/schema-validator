# Server Configuration
PORT=3000

# MongoDB Configuration
# For local MongoDB (Docker Compose or local installation)
MONGODB_URI=mongodb://localhost:27017

# For MongoDB with authentication
# MONGODB_URI=mongodb://username:password@localhost:27017

# For MongoDB Atlas (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/

# Database name
MONGODB_DB_NAME=schema-validator

# RabbitMQ Notification (Optional)
# For local development with Docker Compose:
# RABBIT_MQ_NOTIFICATION_URI=amqp://guest:guest@localhost:5672
# RABBIT_MQ_NOTIFICATION_TOPIC=schema-validator-notifications
# 
# RabbitMQ Management UI: http://localhost:15672
# Default credentials: guest / guest

